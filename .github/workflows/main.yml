name: Drift Resolver CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  drift-check:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: testuser
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: testdb
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Node dependencies
        run: npm install

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install -r drift_resolver/requirements.txt

      - name: Wait for PostgreSQL
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client
          echo "Waiting for PostgreSQL..."
          for i in {1..15}; do
            PGPASSWORD=testpass psql -h localhost -U testuser \
              -d testdb -c 'SELECT 1;' && \
              echo "PostgreSQL is ready." && break
            echo "Attempt $i failed. Retrying in 3s..."
            sleep 3
          done

      - name: Show project structure
        run: |
          echo "=== Migrations ==="
          ls -la prisma/migrations/
          echo "=== Schema ==="
          cat prisma/schema.prisma

      - name: Apply baseline migrations
        run: npx prisma migrate deploy
        env:
          DATABASE_URL: postgresql://testuser:testpass@localhost:5432/testdb

      - name: Confirm clean baseline
        run: npx prisma migrate status
        env:
          DATABASE_URL: postgresql://testuser:testpass@localhost:5432/testdb

      - name: Simulate safe drift — add nullable column
        run: |
          echo "Introducing drift: schema has nullable bio; baseline DB does not."
          echo "Diff will propose ADD COLUMN bio (SAFE)."
          PGPASSWORD=testpass psql \
            -h localhost -U testuser -d testdb \
            -c '\d "User"'
          echo "Drift introduced."

      - name: Confirm drift exists
        run: |
          echo "=== Table structure after drift ==="
          PGPASSWORD=testpass psql \
            -h localhost -U testuser -d testdb \
            -c '\d "User"'
          echo "=== Prisma diff output ==="
          npx prisma migrate diff \
            --from-url "postgresql://testuser:testpass@localhost:5432/testdb" \
            --to-schema-datamodel ./prisma/schema.prisma \
            --script || true
        env:
          DATABASE_URL: postgresql://testuser:testpass@localhost:5432/testdb

      - name: Run Prisma Drift Auto-Resolver
        run: |
          python drift_resolver/main.py \
            --schema ./prisma/schema.prisma \
            --db-url "postgresql://testuser:testpass@localhost:5432/testdb"
        env:
          DATABASE_URL: postgresql://testuser:testpass@localhost:5432/testdb

      - name: Confirm drift was resolved
        if: success()
        run: |
          echo "=== Migration status after resolution ==="
          npx prisma migrate status
          echo ""
          echo "=== Recent migration history ==="
          PGPASSWORD=testpass psql \
            -h localhost -U testuser -d testdb \
            -c 'SELECT migration_name, finished_at FROM "_prisma_migrations"
                ORDER BY finished_at DESC LIMIT 5;'
          echo ""
          echo "=== User table — bio column should now exist ==="
          PGPASSWORD=testpass psql \
            -h localhost -U testuser -d testdb \
            -c '\d "User"'
        env:
          DATABASE_URL: postgresql://testuser:testpass@localhost:5432/testdb

      - name: Show drift report
        if: always()
        run: |
          echo "=== drift-report.json ==="
          cat drift-report.json 2>/dev/null || \
            echo "No report file found."

      - name: Clean up generated drift migrations
        if: always()
        run: |
          echo "Cleaning up auto-generated migration folders..."
          rm -rf prisma/migrations/*_drift_auto_resolve
          echo "Remaining migrations:"
          ls prisma/migrations/

      - name: Upload drift report as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: drift-report-${{ github.run_number }}
          path: |
            drift-report.json
            drift-report.html
          if-no-files-found: warn
